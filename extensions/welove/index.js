var _global = typeof window === 'undefined' ? global : window;


/**
 * !#en
 * Main namespace of Welove, all classes, functions,
 * properties and constants of Welove are defined in this namespace
 * !#zh
 * Welove 的全局的命名空间，
 * 自定义命名空间，单例、常量可以定义在这个命名空间中。
 * @module welove
 * @main welove
 */
_global.welove = require('./welove');