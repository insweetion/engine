var _global = typeof window === 'undefined' ? global : window;

/**
 * !#en
 * Main namespace of Insweetion, all classes, functions,
 * properties and constants of Insweetion are defined in this namespace
 * !#zh
 * Insweetion 的全局的命名空间，
 * 自定义命名空间，单例、常量可以定义在这个命名空间中。
 * @module insweetion
 * @main insweetion
 */
_global.insweetion = require('./insweetion');